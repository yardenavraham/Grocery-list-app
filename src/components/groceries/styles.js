import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({

 title: {
    margin: "1rem 2rem",
    padding: "1rem",
  },
 ulStyle:{
     padding: "0",
 },
 btn:{
    height: '3.5rem',
 }
});

export default useStyles;